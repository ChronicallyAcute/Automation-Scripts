[pytest]
testpaths = tests
# Per-test timeout so a genuine hang fails fast instead of stalling CI.
addopts = -ra --timeout=60 --timeout-method=thread
# conftest.py sets QT_QPA_PLATFORM=offscreen defensively at import time.
